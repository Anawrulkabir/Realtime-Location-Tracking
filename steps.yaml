Steps

1. Check if the browser supports geolocation 2. Set option for accuracy , a 5 second timeout , and no caching . Use watchPosition to crack the users location constantly. Emit the latitude and longitude via socket with "send-location". log any error to console 3. Initialize a map centered at coordinate (0,0 ) with a zoom level of 15 using Leflet. Add OpenStreetMap tiles to the map. 4. Crear an empty object maker, 5. When receiving location data via the socektio , extract id , latitude and longitude and conter the map on the new corrdinates. 5. if a maker for the id exist , update its position , otherwise create a new maker at the given coordinates and add it to the map. When a user disconnects , remove thier make from the map and delete it from makers.
